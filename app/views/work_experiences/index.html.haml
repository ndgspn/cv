%ul.timeline
  - @work_experiences.each do | work |
    %li
      .pull-right.label.label-primary
        = work.start_date
        = "-"
        = work.end_date
      .job-title 
        = work.job_title
      = work.company
